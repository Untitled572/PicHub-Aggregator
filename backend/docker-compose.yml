services:
  pichub:
    build:
      context: ..
      dockerfile: backend/Dockerfile
    network_mode: host
    volumes:
      - ./data:/app/data
      - ./cache:/app/cache
    environment:
      - PORT=5721
      - DB_PATH=/app/data/pichub.db
      - CACHE_PATH=/app/cache
    restart: unless-stopped
